import { Config } from "@src/configuration/Config";

export class Utils {
    public static readonly extensionId = "undefined_publisher.vscode-create";

    /*
    public static createConfig(): Config {
        return new Config(this.extensionId);
    }
    */
}