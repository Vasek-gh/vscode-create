{{#if csproj}}
namespace {{csproj.namespace}}{{replace (replace file.fullDir csproj.directory "") "/" "."}};
{{else}}
namespace {{replace file.fullDir "/" "."}};
{{/if}}

public {{denull csfile.type "class"}} {{file.baseName}}
{
}